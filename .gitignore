# Editor / IDE local workspace files (not for version control)
*.code-workspace

.venv/
venv/
__pycache__/
*.py[cod]
output/
projects/
.DS_Store
.cursor-push-debug.txt
crawl_background.log
crawl_background.pid

# Flask session signing key (generated locally on first run; must not be committed)
secret_key

# PyInstaller build artefacts
build/
dist/
*.app

# Node Playwright (playwright-node/) — HTML report at repo root
playwright-node/node_modules/
playwright-node/test-results/
playwright-report/
playwright-pytest-report.html
